node_modules
.next
dist
coverage
playwright-report
test-results
docs/versions
scripts/node_modules
*.docx
package-lock.json

# generated by next dev; formatting is not ours to control
apps/web/next-env.d.ts

# Prompt templates are program data, not prose. Their exact characters are what
# `prompt_version` names in the call ledger, so a formatter that rewraps one - or
# turns *why* into _why_ - would change what a model reads without changing the
# version that is supposed to identify it. See docs/ADRs/0015-agent-outputs.md.
apps/api/app/agents/prompts
