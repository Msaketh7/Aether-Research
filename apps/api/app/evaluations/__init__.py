"""Benchmark execution and metric aggregation (Phase 18).

Boundary: reads the same telemetry production uses. It never invents a number;
an unmeasured metric is reported as null and rendered as "not measured".
"""
